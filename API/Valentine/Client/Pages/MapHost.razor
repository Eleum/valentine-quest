@page "/map"
@using Valentine.Client.Pages.Modals

<AppKeyModal OnUserLogin="OnUserLoginHandler" />
<MapModal UserId="UserId" />

<Map />

@code {
    private Guid UserId { get; set; }

    public void OnUserLoginHandler(Guid userId)
    {
        UserId = userId;
    }
}